{
  "name": "functions",
  "description": "Cloud Functions for PrivateChat - sends the actual FCM push when a message is written, since the Android client only receives/displays pushes and was never triggering any to be sent.",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.0"
  }
}
